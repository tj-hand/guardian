# ==============================================================================
# Guardian - Docker Compose Environment Variables - EXAMPLE
# ==============================================================================
# Copy this file to .env and customize the values
# This file is safe to commit (no secrets)
#
# NOTE: This file contains Docker Compose configuration ONLY
# For service-specific environment variables, see:
#   - backend/.env.example (Backend/API configuration)
#   - frontend/.env.example (Frontend/UI configuration)
# ==============================================================================

# ==============================================================================
# Docker Image Versions
# ==============================================================================
# Backend Image Configuration
# For development, use 'dev' tag with local builds
# For production, use semantic version tags (v1.0.0, v1.1.0, etc.)
BACKEND_IMAGE=ghcr.io/tj-hand/guardian-backend
BACKEND_VERSION=dev

# Frontend Image Configuration
FRONTEND_IMAGE=ghcr.io/tj-hand/guardian-frontend
FRONTEND_VERSION=dev

# ==============================================================================
# PostgreSQL Database
# ==============================================================================
POSTGRES_USER=guardianuser
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=guardian_db

# Production Database URL (Azure Database for PostgreSQL, AWS RDS, etc.)
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database?ssl=require

# ==============================================================================
# Application Environment
# ==============================================================================
APP_ENV=development

# Security (CHANGE IN PRODUCTION)
SECRET_KEY=generate-a-secure-secret-key-for-production

# Token Configuration
TOKEN_EXPIRY_MINUTES=15
TOKEN_LENGTH=6

# ==============================================================================
# Mailgun Email Service (REQUIRED)
# ==============================================================================
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-domain.com
MAILGUN_FROM_EMAIL=noreply@your-domain.com
MAILGUN_FROM_NAME=Guardian Auth

# ==============================================================================
# CORS Configuration
# ==============================================================================
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080

# ==============================================================================
# Frontend Configuration
# ==============================================================================
VITE_API_BASE_URL=http://localhost:8000/api
VITE_APP_NAME=Guardian
VITE_BRAND_PRIMARY_COLOR=#007bff
VITE_TOKEN_LENGTH=6

# ==============================================================================
# Gateway Configuration (if using a gateway service)
# ==============================================================================
# GATEWAY_IMAGE=ghcr.io/tj-hand/nginx-api-gateway
# GATEWAY_VERSION=latest
# MAX_UPLOAD_SIZE=10M
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS=100
