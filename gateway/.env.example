# Guardian Gateway Configuration

# Backend hosts
BACKEND_1_HOST=guardian-backend
BACKEND_1_PORT=8000

# Frontend (optional - if using Bolt to serve frontend)
FRONTEND_URL=http://localhost:5173

# CORS
CORS_PASSTHROUGH=true

# Gateway secret (optional)
BOLT_GATEWAY_SECRET=
